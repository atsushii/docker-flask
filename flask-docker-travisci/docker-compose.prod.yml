version: '3'
services:
  web:
    build:
      context: .
      dockerfile: Dockerfile.prod
    command: bash entrypoint.sh
    expose:
      - 5000
  nginx:
    build: ./nginx
    ports:
      - 1337:80
    depends_on:
      - web